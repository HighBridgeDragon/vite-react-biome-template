# 規約

## パッケージマネージャ
- bun を使用する（`bun install`, `bun run` など）

## ブランチ命名
- `{ユーザー名またはエージェント名}/{YYYYMMDD}/{説明}` の形式を使用する
- 例: `takaryo/20260329/add-login-page`, `claude/20260329/fix-typo`

## Git 操作
- `git add -A` は禁止。ファイルを個別に指定して add する
- 変更が複数の論理的まとまりにまたがる場合は分割してコミットする
